# position
- static...初期値
- relative...初期値(static)から動かす
- absolute...
  - 何の指定もしないとブラウザで見える領域を基準に配置される
  - relativeを併用すると基準がそこからになる
  (absoluteを指定した要素の基準として指定できるのは、その要素を囲っている要素のみ)
- fix...スクロールしてもついてくる
